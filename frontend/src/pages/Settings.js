import { Link } from 'react-router-dom';

function Settings() {
    return (
        <div>
            <h1>Settings</h1>
            <Link to="/home">BACK</Link>
        </div>
    );
}

export default Settings;